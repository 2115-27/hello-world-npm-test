name: STG_DEPLOY
on:
  workflow_call:
  schedule:
    - cron: '*/5 * * * *'
jobs:        
   Deploy:
    runs-on: ubuntu-latest
    environment: STG
    steps: 
      - run: |
          echo test
          echo stage deployment steps
          

